# Registry entry for the zero-action demo policy (wiring smoke test, no model).
# Sourced by scripts/run_eval.sh — keep it flat KEY=value, no spaces around =.
POLICY_NAME=demo
DESCRIPTION="demo_policy zero-action stub (validates sim+server+client wiring only)"
REPO_DIR=
XPOLICYLAB_POLICY_DIR=XPolicyLab/policy/demo_policy
CONDA_ENV=RoboDojo
DEFAULT_CKPT=demo
ACTION_TYPE=ee
ENV_CFG=arx_x5
